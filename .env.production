# Couple Space 生产环境配置
# 部署到 Manus 托管平台时使用此配置

# 应用配置
APP_ID=couple-space
NODE_ENV=production

# Cookie 密钥（必须修改为随机字符串，建议 32 位以上）
# 生成方法：在终端运行 `openssl rand -base64 32`
COOKIE_SECRET=请替换为你自己的随机密钥-至少32位字符

# 数据库配置
# Manus 会自动注入 DATABASE_URL，无需手动配置
# DATABASE_URL=自动提供

# 邮件服务配置
# Manus 会自动注入 FORGE_API_URL 和 FORGE_API_KEY，无需手动配置
# 使用 Manus 内置邮件服务发送验证码

# 云存储配置（可选）
# 默认使用 Manus 内置存储，后续可切换到阿里云/腾讯云
STORAGE_TYPE=manus

# 阿里云 OSS 配置（可选，如果使用阿里云存储）
# STORAGE_TYPE=aliyun
# ALIYUN_OSS_REGION=oss-cn-hangzhou
# ALIYUN_OSS_ACCESS_KEY_ID=your-access-key-id
# ALIYUN_OSS_ACCESS_KEY_SECRET=your-access-key-secret
# ALIYUN_OSS_BUCKET=couple-space-photos

# 腾讯云 COS 配置（可选，如果使用腾讯云存储）
# STORAGE_TYPE=tencent
# TENCENT_COS_REGION=ap-guangzhou
# TENCENT_COS_SECRET_ID=your-secret-id
# TENCENT_COS_SECRET_KEY=your-secret-key
# TENCENT_COS_BUCKET=couple-space-photos-1234567890
