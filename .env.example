# Couple Space 环境变量配置模板
# 复制此文件为 .env 并填入实际值

# ==================== 数据库配置 ====================
# Manus 托管环境会自动提供 DATABASE_URL
DATABASE_URL=mysql://user:password@host:3306/database

# ==================== 应用配置 ====================
# 应用 ID（用于 JWT）
APP_ID=couple-space

# Cookie 密钥（用于 session 加密，请使用随机字符串）
COOKIE_SECRET=your-random-secret-key-minimum-32-characters

# 服务器端口
PORT=3000

# 运行环境
NODE_ENV=development

# ==================== 邮件服务配置 ====================
# SMTP 服务器地址
SMTP_HOST=smtp.example.com

# SMTP 端口（常用：465/587/25）
SMTP_PORT=465

# 是否使用 SSL/TLS（465端口通常为true，587端口为false）
SMTP_SECURE=true

# SMTP 用户名（通常是邮箱地址）
SMTP_USER=your-email@example.com

# SMTP 密码或授权码
SMTP_PASS=your-password

# 发件人信息
SMTP_FROM=Couple Space <noreply@example.com>

# ==================== 云存储配置 ====================
# 存储类型：manus（默认）| aliyun | tencent
STORAGE_TYPE=manus

# ---------- 阿里云 OSS 配置（STORAGE_TYPE=aliyun 时需要）----------
OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=your-access-key-id
OSS_ACCESS_KEY_SECRET=your-access-key-secret
OSS_BUCKET=your-bucket-name

# ---------- 腾讯云 COS 配置（STORAGE_TYPE=tencent 时需要）----------
COS_SECRET_ID=your-secret-id
COS_SECRET_KEY=your-secret-key
COS_BUCKET=your-bucket-name-1234567890
COS_REGION=ap-guangzhou

# ==================== 可选配置 ====================
# 前端分析工具（可选）
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
